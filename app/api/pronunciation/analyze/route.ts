import { NextResponse } from 'next/server'
import { normalizeAzurePronunciation } from '@/lib/ai/azure-pronunciation-normalizer'
export const runtime='nodejs'
const allowed=new Set(['en-US'])
const json=(value:unknown,status=200)=>NextResponse.json(value,{status})
export async function POST(request:Request){try{const key=process.env.AZURE_SPEECH_KEY,region=process.env.AZURE_SPEECH_REGION;if(process.env.PRONUNCIATION_PROVIDER!=='azure'||!key||!region)return json({provider:'server',status:'not_configured',language:null},503);const form=await request.formData(),audio=form.get('audio'),transcript=String(form.get('transcript')??''),language=String(form.get('language')??'en-US'),consent=form.get('consent')==='true';if(!consent)return json({provider:'server',status:'not_available',language},403);if(!allowed.has(language)||!(audio instanceof File)||!audio.type.startsWith('audio/')||audio.size===0||audio.size>10_000_000||transcript.trim().length<3)return json({provider:'server',status:'not_available',language},400);const assessment=Buffer.from(JSON.stringify({ReferenceText:'',GradingSystem:'HundredMark',Granularity:'Phoneme',EnableProsodyAssessment:true})).toString('base64');const endpoint=`https://${region}.stt.speech.microsoft.com/speech/recognition/conversation/cognitiveservices/v1?language=${encodeURIComponent(language)}&format=detailed`;const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),15000);let response:Response;try{response=await fetch(endpoint,{method:'POST',headers:{'Ocp-Apim-Subscription-Key':key,'Content-Type':audio.type,'Pronunciation-Assessment':assessment,'Accept':'application/json'},body:audio,signal:controller.signal})}finally{clearTimeout(timer)}if(!response.ok)return json({provider:'server',status:'failed',language},response.status===429?429:502);const normalized=normalizeAzurePronunciation(await response.json(),language);return json(normalized,normalized.status==='failed'?502:200)}catch{return json({provider:'server',status:'failed',language:null},502)}}
