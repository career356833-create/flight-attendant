'use client'
import { useEffect, useState } from 'react'
import { createInterviewSession, loadInterviewSessions, type InterviewSession, type InterviewSessionAnalysis, type InterviewSessionMode } from '@/lib/mock-interview-session'
import { interviewQuestionById, type InterviewAttempt, type InterviewQuestion } from '@/lib/interview-practice-data'
const modes:Array<[InterviewSessionMode,string]>=[['general','일반 면접'],['airline_specific','항공사 맞춤'],['english','영어 면접'],['safety','안전 면접']]
const modeLabel:Record<InterviewSessionMode,string>={general:'일반 면접',airline_specific:'항공사 맞춤',english:'영어 면접',safety:'안전 면접',custom:'맞춤 면접'}
export function MockInterviewLauncher({airlineId,onStart,onView,onBack}:{airlineId?:string;onStart:(session:InterviewSession,question:InterviewQuestion)=>void;onView:(session:InterviewSession)=>void;onBack:()=>void}){const [mode,setMode]=useState<InterviewSessionMode>('general'),[count,setCount]=useState<5|8|12>(5),[sessions,setSessions]=useState<InterviewSession[]>([]);useEffect(()=>setSessions(loadInterviewSessions()),[]);const begin=(session:InterviewSession)=>{const question=interviewQuestionById.get(session.questionIds[session.currentQuestionIndex]);if(question)onStart(session,question)},active=sessions.find(s=>s.status==='in_progress'),history=sessions.filter(s=>s.status==='completed').slice(0,5);return <main className="mx-auto max-w-[390px] space-y-4 px-5 pb-8 pt-6"><button onClick={onBack} className="text-sm font-bold text-navy">← 면접 훈련실</button><section className="rounded-3xl bg-navy p-6 text-ivory"><p className="text-xs font-bold text-gold">MOCK INTERVIEW</p><h1 className="mt-2 text-xl font-bold">모의면접 시작</h1><p className="mt-2 text-sm text-ivory/75">여러 질문을 차례로 답하고 세션 리포트를 받아보세요.</p></section>{active&&<section className="rounded-2xl border border-gold bg-card p-4"><h2 className="font-bold text-navy">진행 중인 모의면접이 있습니다.</h2><p className="mt-1 text-sm text-muted-foreground">{active.currentQuestionIndex+1} / {active.questionIds.length} 질문</p><button onClick={()=>begin(active)} className="mt-3 h-11 w-full rounded-xl bg-navy font-bold text-ivory">이어하기</button></section>}<section className="rounded-2xl border border-border bg-card p-4"><h2 className="font-bold text-navy">모드</h2><div className="mt-3 grid grid-cols-2 gap-2">{modes.map(([id,label])=><button key={id} aria-pressed={mode===id} onClick={()=>setMode(id)} className={`min-h-11 rounded-xl border px-3 text-sm font-bold ${mode===id?'border-navy bg-navy text-ivory':'border-border text-navy'}`}>{label}</button>)}</div>{mode==='airline_specific'&&!airlineId&&<p className="mt-3 text-sm text-muted-foreground">목표 항공사를 선택하지 않아 일반 질문으로 진행합니다.</p>}</section><section className="rounded-2xl border border-border bg-card p-4"><h2 className="font-bold text-navy">질문 수</h2><div className="mt-3 grid grid-cols-3 gap-2">{([5,8,12] as const).map(value=><button key={value} aria-pressed={count===value} onClick={()=>setCount(value)} className={`min-h-11 rounded-xl border text-sm font-bold ${count===value?'border-navy bg-navy text-ivory':'border-border text-navy'}`}>{value===5?'Quick 5':value===8?'Standard 8':'Deep 12'}</button>)}</div></section><button onClick={()=>begin(createInterviewSession({mode,airlineId:mode==='airline_specific'?airlineId:undefined,count}))} className="h-12 w-full rounded-2xl bg-coral font-bold text-white">모의면접 시작</button>{history.length>0&&<section className="rounded-2xl border border-border bg-card p-4"><h2 className="font-bold text-navy">최근 모의면접</h2><div className="mt-3 space-y-2">{history.map(session=><button key={session.id} onClick={()=>onView(session)} className="w-full rounded-xl bg-secondary/60 p-3 text-left text-sm"><strong>{session.airlineId?`${session.airlineId} 모의면접`:modeLabel[session.mode]}</strong><span className="mt-1 block text-muted-foreground">{session.questionIds.length}문항 · 완료 · 결과 보기</span></button>)}</div></section>}</main>}
export function MockInterviewProgress({session,onExit}:{session:InterviewSession;onExit:()=>void}){return <div className="mx-auto max-w-[390px] px-5 pt-4"><div className="flex items-center justify-between rounded-2xl bg-navy px-4 py-3 text-sm text-ivory"><span>Mock Interview</span><strong>{Math.min(session.currentQuestionIndex+1,session.questionIds.length)} / {session.questionIds.length}</strong><button onClick={onExit} className="text-gold">나가기</button></div><p className="mt-2 text-xs text-muted-foreground">남은 질문 {Math.max(0,session.questionIds.length-session.currentQuestionIndex-1)}개</p></div>}
export function MockInterviewFollowUp({message,onAnswer,onSkip}:{message:string;onAnswer:()=>void;onSkip:()=>void}){return <main className="mx-auto max-w-[390px] space-y-4 px-5 pb-8 pt-6"><section className="rounded-3xl bg-navy p-6 text-ivory"><p className="text-xs font-bold text-gold">OPTIONAL FOLLOW-UP</p><h1 className="mt-2 text-xl font-bold">한 번 더 깊게 연습해볼까요?</h1><p className="mt-3 text-sm leading-relaxed text-ivory/80">{message}</p></section><button onClick={onAnswer} className="h-12 w-full rounded-2xl bg-coral font-bold text-white">추가 답변하기</button><button onClick={onSkip} className="h-12 w-full rounded-2xl border border-border font-bold text-navy">다음 질문으로</button></main>}
export function MockInterviewReport({session,attempts,onBack,onRetake,onRetry}:{session:InterviewSession;attempts:InterviewAttempt[];onBack:()=>void;onRetake:()=>void;onRetry:(attempt:InterviewAttempt)=>void}){const analysis:InterviewSessionAnalysis=session.sessionAnalysis??{overallSummary:'완료된 답변을 바탕으로 리포트를 준비하고 있어요.',strengths:[],improvements:[],competencySummary:[],speechSummary:'말하기 분석 정보가 없습니다.',audioSummary:'오디오 분석 정보가 없습니다.',contentSummary:'내용 분석 정보가 없습니다.',nextPractice:[]};const rows=attempts.filter(attempt=>session.attemptIds.includes(attempt.id));return <main className="mx-auto max-w-[390px] space-y-4 px-5 pb-8 pt-6"><button onClick={onBack} className="text-sm font-bold text-navy">← 면접 훈련실</button><section className="rounded-3xl bg-navy p-6 text-ivory"><p className="text-xs font-bold text-gold">MOCK INTERVIEW</p><h1 className="mt-2 text-xl font-bold">모의면접 완료</h1><p className="mt-3 text-sm leading-relaxed text-ivory/80">{analysis.overallSummary}</p></section><ReportCard title="잘한 점" items={analysis.strengths} empty="확인 가능한 근거가 쌓이면 이곳에 표시됩니다."/><ReportCard title="가장 먼저 고칠 것" items={analysis.improvements} empty="답변별 개선 제안을 확인해 보세요."/><section className="rounded-2xl border border-border bg-card p-4"><h2 className="font-bold text-navy">역량별 분석</h2><div className="mt-3 space-y-2">{analysis.competencySummary.length?analysis.competencySummary.map(item=><div key={item.competency} className="flex justify-between rounded-xl bg-secondary/60 px-3 py-2 text-sm"><span>{item.competency}</span><strong>{item.strength==='strong'?'강점':item.strength==='partial'?'부분 연결':'보완'}</strong></div>):<p className="text-sm text-muted-foreground">분석 가능한 역량 정보가 없습니다.</p>}</div></section><section className="rounded-2xl border border-border bg-card p-4"><h2 className="font-bold text-navy">말하기 분석</h2><p className="mt-2 text-sm text-muted-foreground">{analysis.speechSummary}</p><p className="mt-1 text-sm text-muted-foreground">{analysis.audioSummary}</p>{analysis.pronunciationSummary&&<p className="mt-1 text-sm text-muted-foreground">{analysis.pronunciationSummary}</p>}</section><ReportCard title="다음 연습 추천" items={analysis.nextPractice} empty="다음 답변을 저장하면 추천이 표시됩니다."/><section className="rounded-2xl border border-border bg-card p-4"><h2 className="font-bold text-navy">질문별 분석</h2><div className="mt-3 space-y-2">{rows.map(attempt=>{const question=interviewQuestionById.get(attempt.questionId);return <details key={attempt.id} className="rounded-xl bg-secondary/60 p-3"><summary className="cursor-pointer text-sm font-bold text-navy">{question?.shortTitle??'면접 질문'}</summary><p className="mt-2 text-sm text-muted-foreground">{attempt.contentAnalysis?.answerSummary.oneLineSummary??attempt.analysis.summary}</p><p className="mt-2 text-xs text-muted-foreground">개선: {attempt.contentAnalysis?.improvementPoints[0]?.message??attempt.analysis.improvements[0]??'기록된 개선점이 없습니다.'}</p><button onClick={()=>onRetry(attempt)} className="mt-3 text-sm font-bold text-navy">다시 답하기</button></details>})}</div></section><button onClick={onRetake} className="h-12 w-full rounded-2xl bg-coral font-bold text-white">이 세션 다시 연습</button></main>}
function ReportCard({title,items,empty}:{title:string;items:string[];empty:string}){return <section className="rounded-2xl border border-border bg-card p-4"><h2 className="font-bold text-navy">{title}</h2>{items.length?<ul className="mt-3 space-y-2 text-sm text-muted-foreground">{items.map(item=><li key={item}>• {item}</li>)}</ul>:<p className="mt-2 text-sm text-muted-foreground">{empty}</p>}</section>}
