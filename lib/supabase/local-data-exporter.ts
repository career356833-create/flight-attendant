export const LOCAL_DATA_KEYS=['cabin.onboarding.v1','cabin-career-experiences','cabin-interview-attempts-v1','cabin-self-introduction-attempts-v1','cabin-application-answers','cabin-learning-analytics-v1','cabin-ai-consent-v1'] as const
export type LocalDataExport={exportVersion:1;exportedAt:string;appVersion:string;data:Record<string,unknown>}
export function exportLocalData():LocalDataExport{const data:Record<string,unknown>={};if(typeof window!=='undefined')LOCAL_DATA_KEYS.forEach(key=>{const raw=localStorage.getItem(key);if(raw)try{data[key]=JSON.parse(raw)}catch{/* skip corrupt entries */}});return{exportVersion:1,exportedAt:new Date().toISOString(),appVersion:'0.1.0',data}}
export function downloadLocalData(){const payload=exportLocalData(),url=URL.createObjectURL(new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download=`cabin-local-data-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url)}
export function summarizeLocalData(){const data=exportLocalData().data;const count=(key:string,field:string)=>{const v=data[key] as Record<string,unknown>|unknown[]|undefined;if(Array.isArray(v))return v.length;const list=v?.[field];return Array.isArray(list)?list.length:0};return{experiences:count('cabin-career-experiences','experiences'),interviews:count('cabin-interview-attempts-v1','attempts'),selfIntroductions:count('cabin-self-introduction-attempts-v1','attempts'),applicationAnswers:count('cabin-application-answers','answers'),weeklyPlans:count('cabin-learning-analytics-v1','weeklyPlans'),audio:0,hasOnboarding:Boolean(data['cabin.onboarding.v1'])}}
