import type { PronunciationAnalysisInput, PronunciationAnalysisResult, PronunciationProvider } from '../pronunciation-provider'
import { aiConsentRepository } from '../consent-repository'
const capabilities={wordAlignment:true,phonemeAlignment:true,wordAccuracy:true,phonemeAccuracy:true,stressAnalysis:false,intonationAnalysis:true} as const
export const azurePronunciationProvider:PronunciationProvider={name:'server',capabilities,isAvailable:()=>typeof window!=='undefined'&&navigator.onLine,async analyze(input){if(!aiConsentRepository.load().allowPronunciationProcessing)return{provider:'server',status:'not_configured',language:input.language};return{provider:'server',status:'not_configured',language:input.language}}}
export async function analyzeAzurePronunciation(audio:Blob,input:PronunciationAnalysisInput,signal?:AbortSignal):Promise<PronunciationAnalysisResult>{if(!aiConsentRepository.load().allowPronunciationProcessing)return{provider:'server',status:'not_configured',language:input.language};const form=new FormData();form.set('audio',audio,'answer.webm');form.set('transcript',input.transcript);form.set('language',input.language??'en-US');form.set('consent','true');try{const response=await fetch('/api/pronunciation/analyze',{method:'POST',body:form,signal});return await response.json() as PronunciationAnalysisResult}catch{return{provider:'server',status:'failed',language:input.language}}}
