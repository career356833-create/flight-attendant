import { failure } from '../errors'
import type { SttProvider } from '../types'
const extension=(mime:string)=>mime.startsWith('audio/mp4')?'mp4':mime.startsWith('audio/mpeg')?'mp3':mime.startsWith('audio/wav')?'wav':'webm'
export const serverSttProvider:SttProvider={id:'server',isSupported:()=>typeof navigator==='undefined'||navigator.onLine,async transcribe(r,c,signal){if(!c.consentGranted)return failure(c.requestId,'consent_required','Audio processing consent required',this.id,false);if(!r.audioBlob||r.audioBlob.size===0)return failure(c.requestId,'invalid_request','Recorded audio is required',this.id,false);try{const mime=r.mimeType||r.audioBlob.type||'audio/webm',form=new FormData();form.append('file',new File([r.audioBlob],`answer.${extension(mime)}`,{type:mime}));form.append('requestId',c.requestId);form.append('languageHint',r.languageHint==='en'?'en':'ko');form.append('consentGranted','true');if(typeof r.durationSeconds==='number')form.append('durationSeconds',String(r.durationSeconds));const response=await fetch('/api/ai/transcribe',{method:'POST',body:form,signal});const body=await response.json().catch(()=>null);if(body&&typeof body==='object')return body;return failure(c.requestId,'invalid_response','Invalid STT server response',this.id,false)}catch(error){if(signal?.aborted)return failure(c.requestId,'timeout','STT request timed out',this.id,false);return failure(c.requestId,'network_error','STT server request failed',this.id,false)}}}
